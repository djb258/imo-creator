# DB Agent Inbox

**Owner**: DB Agent

Receives builder output. Handles database operations (D1, Neon, KV). Passes to Auditor.

**Writes to this directory**: Builder only
**Reads from this directory**: DB Agent only

See `sys/runtime/PACKET_CONTRACT.md` for envelope schema and handoff rules.
