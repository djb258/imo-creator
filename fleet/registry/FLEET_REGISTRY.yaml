# ═══════════════════════════════════════════════════════════════════════════════
# FLEET REGISTRY — Child Repo Inventory
# ═══════════════════════════════════════════════════════════════════════════════
# Authority: Human-maintained (Sovereign)
# Purpose: Single view of all child repos, doctrine versions, sync status
# Location: imo-creator repo root (NOT in templates/ — this is operational)
# Usage: Hand-maintained by human. Audited by scripts/fleet-status.sh
# ═══════════════════════════════════════════════════════════════════════════════
#
# HOW TO USE:
#   - Add a new entry when bootstrapping a child repo
#   - Update child_version after running update_from_imo_creator.sh in child
#   - Run scripts/fleet-status.sh to audit fleet health
#
# RULES:
#   - Only the human adds/removes repos from this file
#   - AI may READ this file but not modify it without explicit permission
#   - Paths must be accessible from imo-creator's location
#
# CANONICAL SOURCE: law/registry/repo_registry.json
#   This file is the human-readable view. repo_registry.json is the
#   machine-readable source used by agents and the dashboard.
#   When in conflict, repo_registry.json is authoritative for paths,
#   branches, and enabled status. This file is authoritative for
#   child_version and last_synced (human-observed values).
#
# ═══════════════════════════════════════════════════════════════════════════════

fleet:
  parent:
    name: "imo-creator"
    current_doctrine_version: "3.5.0"
    manifest_path: "fleet/TEMPLATES_MANIFEST.yaml"

  repos:
    - name: "barton-outreach-core"
      path: "../barton-outreach-core"
      github: "djb258/barton-outreach-core"
      purpose: "Outreach operations hub"
      child_version: "3.5.0"
      last_synced: "2026-03-09"
      status: "ACTIVE"

    - name: "client"
      path: "../client"
      github: "djb258/client"
      purpose: "Client management hub"
      child_version: "3.5.0"
      last_synced: "2026-03-09"
      status: "ACTIVE"

    - name: "company-lifecycle-cl"
      path: "../CL/company-lifecycle-cl"
      github: "djb258/company-lifecycle-cl"
      purpose: "Company lifecycle management hub"
      child_version: "3.5.0"
      last_synced: "2026-03-09"
      status: "ACTIVE"

    - name: "sales"
      path: "../Sales Process"
      github: "djb258/ctb-sales-navigator"
      purpose: "Sales operations hub"
      child_version: "3.5.0"
      last_synced: "2026-03-09"
      status: "ACTIVE"

    - name: "barton-storage"
      path: "../storage container go-nogo"
      github: "djb258/site-scout-pro"
      purpose: "Storage container hub"
      child_version: "3.5.0"
      last_synced: "2026-03-09"
      status: "ACTIVE"

    - name: "research"
      path: "[TBD — no local repo yet]"
      purpose: "Research hub"
      child_version: "[NOT FOUND]"
      last_synced: "[NEVER]"
      status: "PLANNED"

# ═══════════════════════════════════════════════════════════════════════════════
# FIELD REFERENCE
# ═══════════════════════════════════════════════════════════════════════════════
#
# name:           Short repo name (must match directory name)
# path:           Relative path from imo-creator root (or absolute)
# github:         GitHub slug (owner/repo)
# purpose:        One-line description
# child_version:  Doctrine version from child's DOCTRINE.md (update after sync)
# last_synced:    ISO date of last update_from_imo_creator.sh run
# status:         ACTIVE | ARCHIVED | PLANNED
#
# ═══════════════════════════════════════════════════════════════════════════════

document_control:
  created: "2026-02-15"
  last_modified: "2026-03-09"
  maintained_by: "Human only"
