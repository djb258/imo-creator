# HEIR — Hub Environment Identity Record
# Generated by IMO-Creator Garage Fleet Refit Kit
# Do NOT edit manually. Garage validates against sys/contracts/heir.schema.json.

doctrine_version: "{{doctrine_version}}"
sovereign_id: "{{sovereign_id}}"
hub_id: "{{hub_id}}"
ctb_placement: "{{ctb_placement}}"
imo_topology: "{{imo_topology}}"
services:
  - "{{primary_service}}"
secrets_provider: "{{secrets_provider}}"
acceptance_criteria:
  - "{{acceptance_criterion_1}}"
